# Find All
query GetEmployees {
    employees {
        id
        name
        age
        className
        subjects {
            name
        }
        attendance {
            totalDays
            presentDays
        }
    }
}